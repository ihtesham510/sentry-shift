{
	"name": "server",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"build": "tsdown",
		"check-types": "tsc -b",
		"compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
		"dev": "bun run --hot dev.ts",
		"start": "bun run dist/index.mjs"
	},
	"dependencies": {
		"@hono/trpc-server": "^0.4.2",
		"@sentry-shift/api": "workspace:*",
		"@sentry-shift/auth": "workspace:*",
		"@sentry-shift/db": "workspace:*",
		"@sentry-shift/env": "workspace:*",
		"@trpc/server": "catalog:",
		"better-auth": "catalog:",
		"dotenv": "catalog:",
		"hono": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@sentry-shift/config": "workspace:*",
		"@types/bun": "catalog:",
		"tsdown": "^0.22.3",
		"typescript": "^6"
	}
}
